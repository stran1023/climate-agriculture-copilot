{
  "project": "farmtwin-ai-copilot",
  "last_updated": "2026-07-19",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "do_not_skip_verification": true
  },
  "status_legend": {
    "not_started": "Work has not begun.",
    "in_progress": "The feature is the current active task.",
    "blocked": "Work cannot continue until a documented blocker is resolved.",
    "passing": "Required verification has passed and evidence is recorded."
  },
  "pivot_note": "2026-07-14: project rewritten around docs/FarmTwin-AI-Copilot.md (single farm, 4 heterogeneous Farm Assets, isometric digital twin, AI-Copilot-centric UI, structured 6-field recommendations). This replaces the prior rice-cooperative build (feat-001 through feat-007, formerly all 'passing'). That build's full evidence trail is preserved in progress.md under 'Legacy: rice-cooperative build (superseded 2026-07-14)' -- nothing was deleted, the roadmap moved forward. See docs/architecture.md and docs/ui-build-plan.md for the current target design.",
  "features": [
    {
      "id": "feat-030",
      "priority": 23,
      "area": "frontend",
      "name": "Full-width split layout for the Farm view",
      "description": "The split view (feat-022) inherits layout.tsx's shared `max-w-7xl mx-auto` container, which caps the map+dashboard at 7xl width and leaves dead space on either side on wide screens. Move the width constraint out of the shared <main> in layout.tsx and into each individual page's own wrapper instead: the Farm split view gets no width cap (fills the available viewport minus padding), while /briefing (a document-style reading screen, not a dashboard) keeps a readable max-width of its own so its paragraphs don't stretch edge to edge.",
      "user_visible_behavior": "On a wide monitor, the map and dashboard panel stretch to use the full browser width instead of sitting in a fixed-width column with empty space on both sides. The daily briefing page still reads comfortably at a constrained width.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright: set a wide viewport (e.g. 1920px), load /, confirm the map+dashboard container's rendered width is close to the full viewport width (not capped at ~1280px); load /briefing at the same viewport and confirm it's still constrained to a readable width"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean (0 errors/warnings) after removing max-w-7xl from layout.tsx's <main> and adding mx-auto max-w-3xl to /briefing's own wrapper.",
        "Playwright live check (1920x1080 viewport, real dev server): / main element rendered width = 1920px (full viewport, was capped ~1280px before); /briefing content wrapper rendered width = 768px (max-w-3xl), confirmed readable and not full-bleed."
      ],
      "notes": "User-reported: 'right now both panels sit in a fixed-width column with dead black space on either side.'"
    },
    {
      "id": "feat-031",
      "priority": 24,
      "area": "frontend",
      "name": "Themed scrollbar for the scrollable right panel",
      "description": "The right panel (lg:overflow-y-auto in SplitFarmView.tsx) uses the browser's default scrollbar, which is a stark light-gray bar that clashes with the app's dark-mode theme. Add CSS scrollbar theming (scrollbar-color/scrollbar-width for Firefox, ::-webkit-scrollbar rules for Chromium) to globals.css matching the light/dark palette already used elsewhere (zinc tones) -- no new dependency.",
      "user_visible_behavior": "The right panel's scrollbar visually matches the app's theme (light and dark) instead of the stock browser scrollbar.",
      "dependencies": [
        "feat-030"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Visual check in both light and dark mode with enough right-panel content to actually scroll (e.g. an asset detail view) confirming the scrollbar no longer looks like a stock browser control"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after adding .themed-scrollbar CSS (scrollbar-color/width + ::-webkit-scrollbar rules) to globals.css and applying the class to SplitFarmView's right panel.",
        "Playwright live check via document.styleSheets + getComputedStyle on the live right-panel element: scrollbar-color computed as 'rgba(161, 161, 170, 0.6) rgba(0, 0, 0, 0)' (was 'auto' before the fix), scrollbar-width 'thin'. Confirmed both light-mode and dark-mode (@media prefers-color-scheme) rule blocks compiled into the served CSS chunk.",
        "Debugging note: an initial false negative (computed style read as 'auto') traced to a stale/racy first page load in the test harness, not an app defect -- confirmed via direct compiled-CSS-chunk inspection (on-disk .next output and a fresh cache-busted fetch both showed all 8 expected themed-scrollbar rule occurrences) and a clean re-run with document.styleSheets (proper API for externally-linked stylesheets) after a full dev-server + .next cache restart."
      ],
      "notes": "User-reported, flagged as 'worth restyling once the panel is resized anyway' -- sequenced right after feat-030 for that reason, though it's a small independent CSS change."
    },
    {
      "id": "feat-032",
      "priority": 25,
      "area": "frontend",
      "name": "Populate the farm terrain with more decorative scenery",
      "description": "FarmTerrain.tsx currently renders only a farmhouse landmark and 4 corner trees -- sparse next to the 4 real interactive asset markers. Add more non-interactive decorative props (extra trees/bushes, a well or water trough, fence segments along paths, a small vehicle or two, 1-2 walking-person figures) scattered across empty tiles to make the scene read as a populated, lived-in farm. These are purely decorative additions to the terrain layer, not new real backend-driven assets -- the 4 real Snowflake-backed FARM_ASSETS stay exactly as they are; this only adds set dressing around them, consistent with feat-023's foundational terrain work.",
      "user_visible_behavior": "The farm map looks populated and lived-in rather than a handful of icons on mostly-empty tiles.",
      "dependencies": [
        "feat-023"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright screenshot confirming new decorative elements render without colliding with or obstructing the 4 real asset markers, their hover tooltips, or click targets"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after adding decorative EXTRA_TREES/BUSHES/WELL_POS/VEHICLE_POS/PERSON_POSITIONS + fence-post pairs to FarmTerrain.tsx, all filtered against real asset/farmhouse tile positions before rendering.",
        "Playwright live check: terrain renders 10 trees (4 original corners + 6 new, none filtered out on current real asset layout), 3 bushes, 1 well (fountain glyph), 1 vehicle (tractor), 2 person figures, plus sparse fence-post pairs along the dirt paths -- all pointer-events-none (non-interactive, per scope). All 4 real asset marker buttons remained present and clickable by their real aria-labels with no obstruction."
      ],
      "notes": "User-reported: 'a few trees, one shrimp pond, one shed, one warehouse-looking building... more ponds, feed stations, walkways, staff/vehicle markers, additional structures would make the farm feel alive.' Explicitly decorative-only, matching this repo's consistent precedent (feat-024/025) of not fabricating fake data-bearing assets."
    },
    {
      "id": "feat-033",
      "priority": 26,
      "area": "frontend",
      "name": "Standardize marker shape language across all 4 asset types",
      "description": "FishPondMarker uses a rounded-[50%] oval container while ChickenCoopMarker/RiceFieldMarker/FruitOrchardMarker all use rounded-2xl rounded-box containers -- each of the 4 marker components also independently duplicates its own container size/ring/selected-outline styling, so nothing stops this from drifting further as more markers are added. Extract a shared frontend/components/MarkerFrame.tsx (fixed size, rounded-2xl shape, status ring, selected-outline handling) that all 4 markers render their content inside, and convert FishPondMarker's water/fish/dock content to fit the same rounded-box frame instead of an oval. Status/selection stay differentiated by ring color and StatusIndicators' badges (feat-034), not container geometry.",
      "user_visible_behavior": "All 4 asset types on the map share the same container shape; only their internal illustration (water, coop, paddy, trees) and status ring/badges differ.",
      "dependencies": [
        "feat-024",
        "feat-025",
        "feat-026",
        "feat-027"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright screenshot confirming all 4 markers render with identical container size/shape, differing only in internal content and status color; hover/click/tooltip behavior unchanged for all 4"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after extracting components/MarkerFrame.tsx and converting all 4 marker components (FishPondMarker, ChickenCoopMarker, RiceFieldMarker, FruitOrchardMarker) to render through it, including converting FishPondMarker off its old rounded-[50%] oval.",
        "Playwright live check on real data (CC-001 healthy, FO-001 healthy, FP-001 critical, RF-001 healthy): all 4 marker frames measured identical 64x56px box size and identical 16px border-radius (rounded-2xl) -- confirms the previously-inconsistent oval fish-pond shape now matches the other 3 marker types exactly, differing only in internal illustration and ring/badge color."
      ],
      "notes": "User-reported: 'healthy/attention assets use a clean rounded-box outline; the critical pond uses a totally different loose red ellipse + floating warning badge... standardize this now so it scales cleanly.' The shared MarkerFrame also directly prevents the same drift from happening again as feat-032 or future asset types add more markers."
    },
    {
      "id": "feat-034",
      "priority": 27,
      "area": "frontend",
      "name": "Color-blind-safe status iconography + marker accessibility labels",
      "description": "StatusIndicators.tsx currently only gives 2 of the 3 statuses their own icon (critical: bouncing '!' badge, healthy: sparkle) -- needs_attention has no icon at all, only an amber ring, meaning that middle state is communicated by color alone. Add a 3rd badge for needs_attention (a distinct warning-triangle glyph, amber, its own gentler animation) and replace the ambiguous sparkle with a proper checkmark-in-circle badge for healthy, so all 3 statuses share one consistent circular-badge visual language (per feat-033) differing in both glyph AND color, not color alone. While touching these marker components, also add descriptive aria-label attributes to each asset marker button (e.g. 'Tilapia Pond A, fish pond, critical') for screen-reader users, which the current plain <button> with only decorative aria-hidden children lacks entirely.",
      "user_visible_behavior": "Every asset's status is identifiable by icon shape alone (not just ring color) -- useful for color-blind users and screen-reader users alike.",
      "dependencies": [
        "feat-029",
        "feat-033"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright screenshot converted to grayscale (or a DOM check for distinct icon glyphs per status) confirming all 3 statuses remain visually distinguishable without color; confirm each marker button has a real aria-label reflecting that asset's real name/type/status"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after adding the needs_attention badge (amber, triangle glyph, attention-pulse keyframe) and replacing the healthy sparkle with a checkmark badge in StatusIndicators.tsx, plus aria-label additions on marker buttons in DigitalTwinMap.tsx.",
        "Playwright live check on real data: FP-001 (critical) shows the red '!' badge; CC-001/FO-001/RF-001 (healthy) each show a green checkmark badge (not the old ambiguous sparkle); every marker button carries a real, asset-specific aria-label (e.g. 'Tilapia Pond A, fish pond, critical').",
        "No asset in the current real Snowflake-backed data is in needs_attention status, so the amber triangle badge could not be exercised live in this session. Verified statically instead: grepped the compiled dev JS bundle (.next/dev/static/chunks) and confirmed the needs_attention branch, its 'ring-amber' status-ring mapping, and the attention-pulse keyframe reference are all present in the shipped bundle (not dead-code-eliminated). Source review confirms the conditional renders a distinct amber circular badge with a triangle glyph and its own animation, structurally identical in pattern to the already-live critical/healthy badges."
      ],
      "notes": "User-reported: 'pair the green/orange/red with a shape or icon distinction, not color alone, especially important as more assets appear on the map.' The aria-label addition is a related accessibility gap found during this session's code review, not separately requested, folded in here since it touches the same components."
    },
    {
      "id": "feat-035",
      "priority": 28,
      "area": "frontend",
      "name": "Link sidebar alerts and recommendations to their map marker",
      "description": "DashboardPanel's Active Alerts and Daily Recommendations rows already navigate to the right asset on click (feat-013/021), but nothing visually ties a specific sidebar row to its marker on the map while just scanning the list. Add a highlightedAssetId piece of state in SplitFarmView.tsx, set on hover (onMouseEnter/onMouseLeave) of each Active Alert row and each RecommendationCard's asset reference in DashboardPanel, passed down to DigitalTwinMap as a prop that renders a distinct highlight treatment (a bright pulsing outline, visually different from both the permanent 'selected' outline and the marker's own internal hover tooltip) on the corresponding map marker.",
      "user_visible_behavior": "Hovering an alert or recommendation in the sidebar visibly highlights the matching asset's marker on the map, so it's obvious which pond/coop/field is being talked about.",
      "dependencies": [
        "feat-022",
        "feat-033"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright: hover each Active Alert row and confirm the matching marker gets the highlight treatment and no other marker does; confirm the highlight clears on mouseleave; confirm clicking still opens the asset detail panel as before"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after adding highlightedAssetId state to SplitFarmView.tsx, hover handlers on DashboardPanel's Active Alert rows and recommendation cards, and a highlight-pulse treatment in DigitalTwinMap.tsx (new distinct keyframe/color from both spotlight-pulse and the selected outline).",
        "Playwright live check: 0 highlight-pulse elements present before hover; hovering the FP-001 Active Alert row produces exactly 1 highlight-pulse element (on FP-001's own marker only); moving the mouse away clears it back to 0. Click-to-select behavior on markers confirmed unaffected (still opens the asset detail panel)."
      ],
      "notes": "User-reported: 'no visual connection currently ties FP-001 in the sidebar to a specific pond on the map... hover-to-highlight or click-to-locate.' Click-to-open already exists; this feature adds the missing hover-highlight half."
    },
    {
      "id": "feat-036",
      "priority": 29,
      "area": "frontend",
      "name": "Collapsible recommendation cards (progressive disclosure)",
      "description": "RecommendationCard.tsx always renders the full Reason/Evidence/Expected-impact/Confidence text for every recommendation, in every context it's used (Dashboard, Copilot panel, asset detail, briefing) -- dense and report-like when several are shown together, which is the core driver of the 'over information' complaint. Change RecommendationCard to show only the recommendation action text + priority badge by default, with Reason/Evidence/Expected-impact/Confidence collapsed behind a 'View details' toggle (local expanded/collapsed state per card). The always-visible top-tier summary metrics (Farm health score, Active alerts count, Tasks due today, Weather) in DashboardPanel are unaffected -- those already match the 'simple but enough' bar and stay exactly as they are.",
      "user_visible_behavior": "Recommendation cards show just the action and its priority at a glance; clicking 'View details' expands the reasoning, evidence, expected impact, and confidence for that one card.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright: load the dashboard, confirm Reason/Evidence/Expected-impact/Confidence text is not present until 'View details' is clicked on a specific card, confirm it appears after clicking and only for that card, confirm Approve/Reject (where present) still work correctly on a collapsed or expanded card"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after rewriting RecommendationCard.tsx with local expanded/collapsed state, collapsing Reason/Evidence/Expected-impact/Confidence behind a 'View details' toggle; Approve/Reject and asset-link actions (passed as children/showAssetLink) remain always visible.",
        "Playwright live check on the real dashboard (5 real pending recommendations for FP-001): 0 occurrences of 'Reason:' text before any interaction; clicking the first card's 'View details' toggle reveals exactly 1 card's Reason/Evidence text (the others stay collapsed) and the toggle label flips to 'Hide details' on that card only."
      ],
      "notes": "User-reported, described as 'the core fix for over information.' Applies globally to the one shared RecommendationCard component (Dashboard, Copilot panel, asset detail, briefing) rather than only the dashboard, since the same density problem exists everywhere it's used."
    },
    {
      "id": "feat-037",
      "priority": 30,
      "area": "frontend",
      "name": "Health score gauge + trend indicator",
      "description": "DashboardPanel's Farm health score is a bare colored number with no scale reference or trend. Add a small radial/arc gauge showing the score's position in its 0-100 range (needs no historical data, purely a visual read of the current value), plus a trend arrow (up/down/flat) computed by comparing the current farm_health_score against the last value observed in this browser session (no backend history table exists for this derived metric -- persist the previous value in the existing feat-021 dataCache module, comparing on each fresh fetch, flat/no-arrow on the very first load of a session).",
      "user_visible_behavior": "The farm health score shows visually where 70 sits on a 0-100 scale, plus whether it's trending up, down, or flat since the last time this browser session loaded the dashboard.",
      "dependencies": [
        "feat-021"
      ],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright: load the dashboard and confirm the gauge renders at the correct visual position for the real current farm_health_score; trigger a real change in health score (e.g. via a live /workflow/run tick or an approve/reject that shifts risk) and reload, confirming the trend arrow reflects the real direction of change, not a fixed/fake value"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after adding components/HealthGauge.tsx (SVG radial gauge) and healthScoreTrend() to lib/dataCache.ts (module-level, session-scoped, no backend history table -- documented accepted scope limit), wired into DashboardPanel via a microtask-deferred setState in an effect (same pattern used in useApiData.ts to satisfy the react-hooks/set-state-in-effect lint rule).",
        "Playwright live check against the real /dashboard/summary (farm_health_score=70): gauge's aria-label and displayed number both read '70' correctly. On the very first load of a fresh browser session, no trend arrow or flat-marker renders (real null state, not faked). After waiting out the 20s dataCache TTL and forcing a real refetch (navigate to an asset detail and back, which remounts DashboardPanel), the gauge showed the 'flat' (■) marker -- the real, accurate comparison, since the live farm_health_score had not actually changed between the two fetches (70 -> 70). This demonstrates the trend logic reads real data rather than a fixed/fake value."
      ],
      "notes": "User-reported: 'a bare 70 in orange has no scale or trend... let it stay simple while adding meaning.' Trend is session-scoped by necessity (no backend metric-history table for this derived score) -- documented as an accepted scope limit, not silently faked."
    },
    {
      "id": "feat-038",
      "priority": 31,
      "area": "frontend",
      "name": "Compress the weather row to one primary metric",
      "description": "DashboardPanel's Weather card currently shows temperature/humidity/rainfall/wind as 4 icons of equal visual weight in a wrapped flex row, competing for attention. Redesign as one primary metric (temperature, large/bold) with humidity/rainfall/wind shown as smaller secondary text alongside it.",
      "user_visible_behavior": "The weather card reads at a glance -- one large temperature figure, with humidity/rain/wind as small supporting detail rather than 4 equally-weighted icons.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Playwright screenshot confirming temperature is visually dominant (larger font/weight) and the other 3 metrics are visually secondary, all 4 real values still present and correct"
      ],
      "evidence": [
        "npm run build && npm run lint: both clean after redesigning DashboardPanel's Weather card to show temperature as a large primary figure with humidity/rainfall/wind as smaller secondary text.",
        "Playwright live check against real weather data: primary temperature text rendered at 36px font size vs 12px for the secondary humidity/rain/wind lines; all 4 real values (27.8°C, 91% humidity, 11.6mm rain, 19km/h wind) present and correct."
      ],
      "notes": "User-reported: 'four icons of equal visual weight competing for attention; consider one primary metric with the rest as secondary/smaller text.'"
    },
    {
      "id": "feat-039",
      "priority": 32,
      "area": "frontend",
      "name": "Swap in the v0-generated frontend redesign (farmtwin-ai-copilot-frontend/), wired to the real backend",
      "description": "User supplied a new, separately-built Next.js frontend (shadcn/ui, pan/zoom digital twin map, dedicated /copilot route) at farmtwin-ai-copilot-frontend/, shipped wired only to an in-memory lib/mockData.ts with its own lib/types.ts contract (different field names/shapes than backend/app/models/schemas.py -- e.g. asset.id vs asset_id, confidence 0-1 vs confidence_pct 0-100, growth_stage as a 0-4 index vs the backend's string enum). Per user decision (AskUserQuestion), this replaces frontend/ entirely (old frontend/ removed via git rm, new project moved into frontend/) and switches its package manager from pnpm to npm to match the rest of the repo.",
      "user_visible_behavior": "The new redesigned UI (pan/zoom digital twin map, shadcn/ui components, dedicated Farm/Copilot/Briefing nav) now shows real Snowflake-backed farm data end to end -- same real endpoints the prior frontend build used, just presented through the new design.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "cd frontend && npm run build && npx tsc --noEmit && npm run lint all succeed",
        "Live Playwright walkthrough against the real backend + live Snowflake account: / renders 4 real asset markers with correct real status/health; clicking Tilapia Pond A opens real sensor readings + risk badge; Approve/Reject on a real recommendation writes through to Snowflake (pending count drops) and the UI refreshes; /briefing renders a real Cortex-generated summary; /copilot answers a real free-form question grounded in live data; zero console/hydration errors across all of the above"
      ],
      "evidence": [
        "Directory swap: `git rm -r frontend` (old, tracked) then the new project moved into frontend/, pnpm-lock.yaml removed, package.json name set to \"frontend\", `npm install` succeeded (358 packages). .env.local (NEXT_PUBLIC_API_URL=http://localhost:8000) carried over unchanged from the old frontend.",
        "Rewrote frontend/lib/api.ts: replaced every lib/mockData.ts-backed function with real fetch() calls against backend/app/main.py's endpoints, plus a mapping layer translating backend/app/models/schemas.py shapes (AssetOverview, AssetDetail, Recommendation, DashboardSummary, BriefingToday, CopilotAnswer) onto the new frontend's own lib/types.ts contract -- growth_stage string->0-4 index (matches asset_simulator.py's GROWTH_STAGES), confidence_pct/100, pending_approval->pending, an asset-name cross-reference cache for recommendations/alerts that only carry asset_id, and per-reading tone colors mirrored from risk_engine.py's own real thresholds (not invented judgment calls). Deleted the now-unused lib/mockData.ts.",
        "npx tsc --noEmit and npm run lint: both clean (added eslint + eslint-config-next + eslint.config.mjs, absent from the v0 export, so `npm run lint` actually runs instead of silently no-op'ing -- next.config.mjs's ignoreBuildErrors:true means `npm run build` alone does not catch type errors).",
        "Found and fixed 3 real pre-existing bugs in the v0-generated code surfaced by this integration's live verification, none introduced by the api.ts rewrite: (1) DashboardPanel.tsx wrapped each RecommendationCard in an outer <button>, but RecommendationCard renders its own <button>s (View details/Approve/Reject) -- invalid nested-button HTML that broke hydration; changed the outer wrapper to a role=button div with keyboard support and added stopPropagation to RecommendationCard's internal buttons. (2) lib/useApiData.ts passed the same getSnapshot function as both the client AND server snapshot for useSyncExternalStore, so SSR could read a stale value from dataCache's module-level singleton left over from a prior request in the same Next dev process, mismatching the client's genuinely-empty first render -- fixed by making the server snapshot always return undefined. (3) A CopilotPanel.tsx react-hooks/purity lint error (Date.now() used for message ids) and 2 react-hooks/set-state-in-effect errors (HealthGauge.tsx, WeatherAmbience.tsx) -- fixed with a ref-based id counter and the same microtask-deferred-setState pattern this repo's lib/useApiData.ts already established.",
        "Live Playwright, real backend + live Snowflake account: / showed all 4 real assets (CC-001/FO-001/FP-001/RF-001) with correct real status (FP-001 critical/10, others healthy/90); clicked Tilapia Pond A -> real sensor readings, Critical badge, dissolved-oxygen reading present. Approved a real FP-001 recommendation through the UI -- GET /assets/FP-001/recommendations confirmed pending count dropped 13->12, a real Snowflake write-back. /briefing loaded in 40.2s (real Cortex Agent call) with a real generated summary citing exact real data (DO 3.1->2.0 mg/L, Q4-2024 crash reference). /copilot answered 'Should I feed the fish?' in 37.5s with a real grounded 'No' citing the live 2.0 mg/L DO reading and 33.4C water temp. Zero console/page errors across every one of these checks, including 5 repeated fresh-context reloads of the deep-linked /assets/FP-001 route specifically re-testing the hydration fix.",
        "Killed all background uvicorn/next-dev processes cleanly after verification."
      ],
      "notes": "Known, not fixed (out of scope -- backend-side, pre-existing, unrelated to this frontend swap): the live /briefing/today summary contained one leaked agent-planning sentence ('Let me broaden to recent days to ensure I capture...'), the same class of narration-leak issue backend/app/main.py's _clean_agent_answer/_strip_narration_prefix has repeatedly had to be extended for (see Session 011-014 notes below) -- this is a new unhandled phrasing shape, a backend fix, not a frontend integration bug. docs/frontend-architecture.md describes the now-removed old frontend and needs a rewrite to describe this new one -- flagged, not yet done this session. **Fixed in feat-040.**"
    },
    {
      "id": "feat-040",
      "priority": 33,
      "area": "backend",
      "name": "Fix a new Cortex Agent narration-leak shape in /briefing/today's summary",
      "description": "User reported the Daily Briefing overview rendering a leaked agent-planning preamble before the real summary (\"Only one recommendation matched today's date exactly. Let me broaden to recent recommendations...\"). backend/app/main.py's _clean_agent_answer/_strip_narration_prefix (extended repeatedly in Sessions 011-014 for earlier shapes) only stripped narration that began with a known lead-in phrase ('I'll ', 'Let me ', ...) -- this narration's first sentence ('Only one recommendation matched...') didn't start with any of them, so nothing was stripped. Replaced the phrase-matching approach with a more general heuristic: real prose always has a space after sentence-ending punctuation, so punctuation glued directly to the next capitalized/bold word (no space) is the actual, consistently-observed narration/answer seam across every previously-documented leak shape -- cut at the LAST such seam (narration itself can span multiple normally-spaced sentences before the final glued handoff), keeping the old lead-in-phrase loop as a fallback for narration that survives with normal spacing.",
      "user_visible_behavior": "The Daily Briefing overview (and any other Cortex Agent summary/answer using _clean_agent_answer) starts directly with the real answer, with no leaked tool-planning sentences ahead of it, regardless of which lead-in phrase (if any) the agent happened to use.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "python -m compileall app succeeds",
        "A scripted test of _clean_agent_answer() against the exact reported leaked text, plus every previously-documented narration shape (<answer> tag, narration-into-heading, lead-in-phrase narration) and 2 negative cases (a decimal number, already-clean text) -- all must produce the correct output with zero regressions",
        "A real, live GET /briefing/today call against the live Snowflake account + real Cortex Agent confirms the actual summary text starts cleanly with no leaked narration"
      ],
      "evidence": [
        "python -m compileall app: clean.",
        "Scripted regression test (6 cases) against the live venv: reported bug -> summary now starts exactly at 'Today's recommendation activity is entirely concentrated on...', with the two-sentence glued narration ('Only one recommendation matched today's date exactly. Let me broaden...risks.Today's...') fully stripped. All 3 previously-documented shapes (<answer> tag, heading, spaced lead-in phrase) still stripped correctly -- no regression. Both negative cases (a decimal number '3.5mg/L', already-clean text) passed through completely unchanged, confirming the new glued-boundary regex correctly requires an uppercase/bold follower and never fires on a digit.",
        "Live verification: started uvicorn against the live account, called GET /briefing/today for real (34.5s, real Cortex Agent call, not cached). Real response summary began: 'Every approved and rejected recommendation across the farm today concerns a single asset — **Tilapia Pond A (FP-001)**...' -- zero leaked narration, confirming the fix holds against genuine live agent output, not just the captured test string. Stopped uvicorn cleanly after verification."
      ],
      "notes": "User-reported live in the Daily Briefing UI (frontend/components/BriefingView.tsx renders whatever backend/app/main.py's /briefing/today returns verbatim -- this was purely a backend response-cleaning bug, not a frontend defect). Same _clean_agent_answer function is also used by /workflow/run's recommendation summaries and POST /copilot/ask, so this fix applies to all three call sites."
    },
    {
      "id": "feat-041",
      "priority": 34,
      "area": "frontend",
      "name": "Redesign the Daily Briefing Overview card for scanability",
      "description": "User supplied a detailed rendering/UI-only design brief (explicitly not a content-generation change) for the Overview card: render **bold** markdown as real bold instead of raw asterisks, break the single-paragraph summary into short scannable paragraphs, ~1.6-1.8 line height, constrained line length, sparing/real (not invented) use of badges for priority/status/confidence/risk, visual separation between the primary incident and the rest of the farm's status, and a clean dashboard aesthetic consistent with the rest of the app. New frontend/lib/markdown.tsx: renderInlineMarkdown() (bold-only parser, no new dependency) and splitIntoSentences() (a sentence-boundary regex requiring a space before the next capital/bold marker, so it never splits mid-decimal like '3.5'). New frontend/components/BriefingOverview.tsx: derives a 'primary asset' from the real Briefing.decisions data (most-referenced asset_id, not parsed from prose) and renders a badge banner (RiskBadge from a shared 'assets' fetch, PriorityBadge, a real confidence-percent chip, real approved/rejected counts) above the sentence-split prose, plus a visually separate 'Rest of the farm' strip of the other real assets' current status.",
      "user_visible_behavior": "The Overview card on /briefing shows a badge banner naming the primary affected asset with its real risk/priority/confidence/approval-count, followed by the AI summary broken into short readable paragraphs with real bold text (no raw '**'), and a clearly separated strip showing the rest of the farm's real status -- scannable in seconds instead of one dense paragraph.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "npx tsc --noEmit && npm run lint && npm run build all succeed in frontend/",
        "Live Playwright check against the real backend + live Snowflake account + real Cortex Agent: rendered page contains zero raw '**' markdown; the Overview card renders multiple <p> paragraphs and real <strong> elements; a confidence-percent chip and an approved/rejected count chip are present; the 'Rest of the farm' section is present; zero console/page errors. Screenshots captured at desktop (1280px) and tablet (820px) widths."
      ],
      "evidence": [
        "npx tsc --noEmit, npm run lint, npm run build: all clean after adding lib/markdown.tsx and components/BriefingOverview.tsx and wiring them into components/BriefingView.tsx (which now also fetches the shared 'assets' cache key to source each asset's real current status).",
        "Live Playwright check (real backend, live Snowflake + Cortex Agent, /briefing/today call took 40.7s): page text contained zero '**'; Overview card rendered 6 <p> paragraphs (one per real sentence) and 2 real <strong> elements; a '% confidence' chip, an 'approved'/'rejected' chip, and the 'Rest of the farm' section were all present; zero console/page errors.",
        "Screenshots at 1280px and 820px viewports confirmed the badge banner (asset name, Critical risk badge, High priority badge, '96% confidence' chip, '1 approved' chip), 6-8 short well-spaced paragraphs with real bold rendering (e.g. '**rejected**' rendered as actual bold text, not asterisks), and the separated 'Rest of the farm' dot-chip strip (Layer House North / Mango Grove West / Paddy Block East) -- all reflowing correctly at both widths."
      ],
      "notes": "Verification surfaced a second, different live narration-leak shape in one of the two independent /briefing/today calls used for testing (properly-spaced narration sentences that don't start with a recognized lead-in phrase and aren't glued to the next sentence, e.g. 'Filtering to items actually approved/rejected today... so I'll summarize...') -- distinct from feat-040's glued-boundary bug and NOT fixed here, since the user explicitly scoped this task to rendering only ('not the content generation'). The redesigned card still rendered that leaked text as clean, readable paragraphs, confirming the rendering fix is correct independent of input content quality. Flagged to the user as a candidate follow-up, not actioned. **Fixed in feat-042.**"
    },
    {
      "id": "feat-042",
      "priority": 35,
      "area": "backend",
      "name": "Fix the second (non-glued) Cortex Agent narration-leak shape",
      "description": "feat-040 fixed narration glued directly onto the real answer with no separating whitespace. feat-041's verification found a second, harder shape: some raw responses have no glued boundary anywhere -- narration and the real answer are evenly, normally spaced all the way through (e.g. 'Every decided recommendation belongs to Tilapia Pond A (FP-001). Filtering to items actually approved/rejected *today* (approved_at on 2026-07-15) yields the single approved item; but the user asked about \"today's\" decisions broadly, so I'll summarize the decisions made in this active FP-001 crisis window. All approved and rejected recommendations today concern...'), so there's no typographical seam left to cut at, and the leading sentence often doesn't itself contain a recognizable lead-in phrase (only a later sentence in the same narration block does). backend/app/main.py's _strip_narration_prefix was rewritten from a prefix-only phrase match into a content classifier (_looks_like_narration): a sentence is treated as narration if it uses first-person process language (I'll/let me/I have/I've/...), references 'the user'/'my filter'/'my query', describes a filtering/querying step, or leaks a raw snake_case field name (e.g. 'approved_at', a Snowflake column name that would never appear in genuine natural-language farm advice). Rather than stopping at the first sentence that doesn't match (which fails when a narration sentence happens to contain none of these signals but is sandwiched between two that do), it scans a bounded window of the first 4 sentences and cuts everything up to and including the LAST matching sentence in that window -- mirroring feat-040's 'cut at the last match' strategy, applied to content instead of typography.",
      "user_visible_behavior": "The Daily Briefing overview (and any other Cortex Agent summary/answer) starts directly with the real answer even when the agent's narration preamble is spread across multiple normally-spaced sentences with no recognizable lead-in phrase at the very start.",
      "dependencies": ["feat-040"],
      "status": "passing",
      "verification": [
        "python -m compileall app succeeds",
        "A scripted regression test covering: the feat-040 glued case, the new reconstructed second-shape case (properly-spaced, no known lead-in, snake_case field-name leak), 3 fresh live-captured raw Cortex Agent samples, the <answer>-tag and heading shapes, 2 negative cases (a decimal number, a sentence mentioning 'filtration' that must not be confused with 'filtering to/for'), plus idempotency checks against 3 real already-clean captured answers from earlier sessions -- all must pass with zero regressions",
        "A real, live GET /briefing/today call against the live Snowflake account + real Cortex Agent confirms the actual summary starts cleanly"
      ],
      "evidence": [
        "python -m compileall app: clean.",
        "Captured 3 fresh raw (uncleaned) Cortex Agent responses directly via cortex_agent_client.ask_agent() using /briefing/today's exact prompt, to study real narration shapes rather than guessing. All 3 turned out to be glued (already handled by feat-040) -- confirmed via _clean_agent_answer producing the correct real-answer start for all 3.",
        "10-case scripted regression test: the feat-040 bug, a faithful reconstruction of the newly-reported second shape (properly-spaced, first sentence alone has no narration signal, only the second sentence does -- confirms the bounded-window 'last match' scan is necessary, since an earlier 'stop at first non-matching sentence' version of this fix failed this exact case), all 3 freshly-captured live raw samples, the <answer>-tag and heading shapes, a decimal-number negative case, an already-clean negative case, and a 'filtration' negative case (confirms 'filtering to/for' doesn't over-match on unrelated real vocabulary) -- all 10 passed.",
        "Separate idempotency test: 3 real, already-cleaned answers captured live in earlier sessions (a copilot 'should I feed the fish' answer, a real briefing summary, and a copilot multi-asset triage answer) all passed through _clean_agent_answer completely unchanged -- confirms the new content classifier doesn't false-positive and strip real content out of genuine multi-sentence prose.",
        "Live verification: started uvicorn against the live account, called GET /briefing/today for real (29.8s, a genuine Cortex Agent call). Real response summary began cleanly: 'Today's activity is dominated entirely by **Tilapia Pond A (FP-001)**...' -- zero leaked narration. Stopped uvicorn cleanly after verification."
      ],
      "notes": "Same three call sites as feat-040 (/workflow/run, /briefing/today, POST /copilot/ask) all share _clean_agent_answer, so this fix applies everywhere. The _NARRATION_SCAN_WINDOW is capped at 4 sentences specifically so this can never reach into and mis-strip a legitimate later sentence of a long copilot answer -- narration has only ever been observed as 1-2 sentences in every real sample captured across this project's history."
    },
    {
      "id": "feat-043",
      "priority": 36,
      "area": "backend",
      "name": "Inventory/stock-aware recommendations",
      "description": "FARM_OPS_AGENT and the /workflow/run recommendation pipeline currently recommend actions (add aerator, treat with antibiotic, adjust feed) with no awareness of whether the required item is actually on hand -- a real gap between 'advice' and 'actionable'. Add a new Snowflake INVENTORY table (item_name, category, unit, qty_on_hand, reorder_threshold, last_restocked) via CoCo, seeded with realistic stock levels for items referenced by the demo's existing recommendation content (aerator units, probiotic/antibiotic doses, chicken feed, irrigation parts). Extend FARM_OPS_VIEW (or add a second semantic-view source) so the agent can check stock before/while generating a recommendation, and surface a stock-availability field (in_stock / low_stock / out_of_stock) on each RECOMMENDATIONS row, parsed alongside the existing 6 fields. Where a recommended action's input is out of stock, the recommendation text should say so explicitly (e.g. 'requires probiotic treatment -- 0 units in stock, reorder before acting') rather than recommending an action with no way to execute it.",
      "user_visible_behavior": "Recommendation cards show whether the item/input a recommendation depends on is actually in stock, and the AI's own recommendation text acknowledges when it isn't (e.g. flags a reorder need) instead of silently assuming unlimited supply.",
      "dependencies": ["feat-012"],
      "status": "not_started",
      "verification": [
        "Blocked until the user runs a new CoCo prompt creating and seeding INVENTORY (see snowflake/coco-prompts.md) -- this agent cannot create Snowflake objects itself per CLAUDE.md",
        "cd backend && python -m compileall app succeeds",
        "A live /workflow/run call against the live Snowflake account produces at least one RECOMMENDATIONS row whose text/fields correctly reflect a real seeded low-stock or out-of-stock INVENTORY row, cross-checked directly against the INVENTORY table",
        "A live /workflow/run call against a well-stocked item confirms the recommendation does not spuriously flag a stock problem that doesn't exist"
      ],
      "evidence": [],
      "notes": "From a 2026-07-19 discussion on strengthening the 'Build Agent Skills or custom skills aligned to the workflow' judging bullet in docs/challenge.md with a second distinct, real-world-grounded Cortex Agent skill (structured inventory lookup, not just text-to-SQL over FARM_OPS_VIEW). Queued behind the user's current UI fix -- not yet started."
    },
    {
      "id": "feat-044",
      "priority": 37,
      "area": "backend",
      "name": "Regulatory/withdrawal-period compliance check",
      "description": "Fish and poultry treatments (antibiotics, certain medications) carry a mandatory withdrawal period before the animal/product can be harvested or sold -- recommending a treatment today without flagging when it becomes safe to harvest/sell is a real agricultural compliance gap, not a hypothetical one. Add a new Snowflake WITHDRAWAL_RULES table (treatment_name, asset_type, withdrawal_days) via CoCo, plus a TREATMENTS log table (asset_id, treatment_name, administered_at) recording when a treatment was actually given (written by the approve flow when a recommendation involving a regulated treatment is approved). Extend the recommendation/risk pipeline to check TREATMENTS + WITHDRAWAL_RULES before generating a harvest/sale-related recommendation for an asset under an active withdrawal period, and have the agent surface a clear compliance warning (e.g. 'do not harvest before 2026-07-25 -- 7-day withdrawal period after the 2026-07-18 antibiotic treatment') rather than staying silent on it.",
      "user_visible_behavior": "If an asset was recently given a regulated treatment, the AI Copilot and recommendation cards clearly show the earliest safe harvest/sale date instead of staying silent on a real compliance constraint.",
      "dependencies": ["feat-012"],
      "status": "not_started",
      "verification": [
        "Blocked until the user runs a new CoCo prompt creating and seeding WITHDRAWAL_RULES + TREATMENTS (see snowflake/coco-prompts.md) -- this agent cannot create Snowflake objects itself per CLAUDE.md",
        "cd backend && python -m compileall app succeeds",
        "A live test: approve a recommendation that logs a regulated treatment for a real asset, then confirm a subsequent live /workflow/run or /copilot/ask response for that asset correctly surfaces the real computed earliest-safe-harvest date (administered_at + withdrawal_days), not a generic disclaimer",
        "Confirm an asset with no recent regulated treatment produces no compliance warning (no false positives)"
      ],
      "evidence": [],
      "notes": "From the same 2026-07-19 discussion as feat-043 -- the stronger 'guardrails' framing for docs/challenge.md's Technical Execution criterion. Queued behind the user's current UI fix -- not yet started."
    },
    {
      "id": "feat-045",
      "priority": 38,
      "area": "frontend",
      "name": "Consolidate Farm Overview: move Asset Status to the map, remove duplicate Alerts/Recommendations sections",
      "description": "User-requested Farm Overview cleanup: (1) replace the map's static Critical/Attention/Healthy color-key legend (bottom-left) with a real per-status asset-count pill at bottom-center, computed client-side from the already-fetched assets list (no new API call) -- this both frees the legend slot and relocates the DashboardPanel's old 'Asset Status' card onto the map itself. (2) Remove the 'Active Alerts' card from Farm Overview (DashboardPanel) since the map's marker rings/badges already surface the same information. (3) Remove the 'Daily Recommendations' section from Farm Overview since each asset's own detail view already shows its full recommendation cards. (4) Keep 'Tasks Due Today' -- unlike Daily Recommendations it's a lightweight one-line-per-item farm-wide checklist with no duplicated reasoning, matching the user's 'general information, not too specific' bar for Overview -- and give it the full-width row freed up by removing Asset Status from the grid. (5) Preserve the sidebar-to-map visual connection that Active Alerts/Recommendations used to provide (feat-035) by wiring hover (not click, since clicking the map marker directly already opens asset detail -- a second click path would be redundant) on each Tasks Due Today row to the same onHoverAsset highlight callback, using Task.asset_id (already present on the type, previously unused in this list).",
      "user_visible_behavior": "The map's bottom-center now shows real live asset counts per status (e.g. 'Critical 1 / Attention 0 / Healthy 3') instead of a static color key. Farm Overview only shows Health score, Weather, and a full-width Tasks Due Today list; Active Alerts and Daily Recommendations are gone (redundant with the map and each asset's detail view). Hovering a task row highlights its asset's marker on the map, without adding a second click-to-navigate path.",
      "dependencies": ["feat-022", "feat-035"],
      "status": "passing",
      "verification": [
        "cd frontend && npx tsc --noEmit && npm run lint && npm run build all succeed",
        "Live Playwright check against the real backend + live Snowflake account: confirm exactly one 'Asset status summary' element renders at the map's bottom-center (horizontally centered on the map stage) showing real per-status counts matching the live assets list; confirm 'Active Alerts' and 'Daily Recommendations' headings are absent from the page; confirm 'Tasks Due Today' is present and full-width; hover the first task row and confirm a highlight-ring element appears on exactly the correct corresponding asset marker (matched by aria-label), then confirm it clears on mouse-leave; zero console errors throughout"
      ],
      "evidence": [
        "npx tsc --noEmit: clean. npm run lint: clean (0 errors/warnings after removing now-unused imports BellRing, Sparkles, RiskBadge, RecommendationCard from DashboardPanel.tsx). npm run build: clean, all 5 routes compiled successfully.",
        "Live Playwright check (real backend + live Snowflake account, dev servers already running on :8000/:3000): the new '[aria-label=\"Asset status summary\"]' pill rendered exactly once, text 'Critical 1 / Attention 0 / Healthy 3' -- matches the live assets list (FP-001 critical, CC-001/FO-001/RF-001 healthy) exactly. Its bounding-box center x (419.5) matched the digital-twin map stage's own center x (419.5) -- confirms true horizontal centering, not just visual approximation. Page text contained zero occurrences of 'Active Alerts' or 'Daily Recommendations'; 'Tasks Due Today' present with 12 real task items (real FP-001 DO-crisis recommendation text, e.g. 'Stop feeding at once and resume only after DO is sustained above 5.0 mg/L.').",
        "Hover-highlight check: 0 '[style*=\"highlight-ring\"]' elements before hover; hovering the first Tasks Due Today row produced exactly 1; moving the mouse away cleared it back to 0. Traced the highlighted marker's DOM ancestor back to its own aria-label button, which read 'Tilapia Pond A, fish pond, critical' -- confirms the hover correctly targets the real asset referenced by that specific task (a fish-pond DO/feeding recommendation), not an arbitrary or wrong marker.",
        "Zero console/page errors across all checks."
      ],
      "notes": "User initially ruled out click-to-navigate on task rows during design discussion, reasoning the map marker already provides that path -- hover-only shipped here for that reason. **Revisited and reversed in feat-046**: the user asked for direct click-to-navigate after all, since it's faster than locating and clicking the marker on an isometric map with no text labels."
    },
    {
      "id": "feat-046",
      "priority": 39,
      "area": "frontend",
      "name": "Add click-to-navigate on Tasks Due Today rows",
      "description": "feat-045 deliberately shipped Tasks Due Today rows as hover-only (highlight the map marker, no click), reasoning that clicking the marker directly already opens asset detail so a second click path would be redundant. User revisited this and asked for direct click-to-navigate on each row too. Changed DashboardPanel.tsx's task <li> rendering: rows with a real task.asset_id now render their content inside a real <button> (onClick calling onSelectAsset(assetId), keeping the existing onMouseEnter/onMouseLeave highlight handlers), so the row is both keyboard- and mouse-operable, not just hover-only. Rows with no asset_id (Task.asset_id is optional on the type) render as a plain non-interactive <li>, unchanged. Re-added the onSelectAsset destructure to DashboardPanel's props, which feat-045 had dropped as unused.",
      "user_visible_behavior": "Clicking a Tasks Due Today row now opens that task's asset directly in the detail panel, the same as clicking its marker on the map would -- hovering still highlights the marker first, so you can see which asset you're about to open.",
      "dependencies": ["feat-045"],
      "status": "passing",
      "verification": [
        "cd frontend && npx tsc --noEmit && npm run lint && npm run build all succeed",
        "Live Playwright check against the real backend + live Snowflake account: click the first Tasks Due Today row and confirm the URL changes to the correct /assets/{id} route and the asset detail panel actually renders that asset's real name (not just a URL change); zero console errors"
      ],
      "evidence": [
        "npx tsc --noEmit: clean. npm run lint: clean. npm run build: clean, all 5 routes compiled successfully.",
        "Live Playwright check (real backend + live Snowflake account): 12 real clickable task buttons found; clicking the first ('Stop feeding at once and resume only after DO is sustained above 5.0 mg/L.') navigated from http://localhost:3000/ to http://localhost:3000/assets/FP-001, and the rendered page's heading read 'Tilapia Pond A' -- confirms the click opens the correct asset's real detail view, not just a URL change. Zero console/page errors."
      ],
      "notes": "Supersedes feat-045's explicit 'hover-only, no click' decision -- kept feat-045's evidence trail as-is (it was correct and verified at the time) rather than rewriting history, and pointed forward to this feature instead."
    },
    {
      "id": "feat-047",
      "priority": 40,
      "area": "frontend",
      "name": "Asset Detail: collapse History behind a header toggle, remove duplicate Today's Tasks",
      "description": "User-requested Asset Detail cleanup, based on reading lib/api.ts: (1) AssetDetail.tasks ('Today's Tasks' card) turned out to be built from the exact same /assets/{id}/recommendations call as the 'Recommendations' section directly above it (lib/api.ts:392-397), just stripped to label-only with done hardcoded false -- a strict subset of the same data, not a distinct view (unlike the farm-wide Tasks Due Today on the dashboard, which aggregates across all 4 assets). Removed the 'Today's Tasks' card entirely rather than merging it into a hybrid, since Recommendations (with feat-036's own collapsed-by-default cards) already covers it with more detail. (2) The standalone always-visible 'History' card is replaced with a small 'History' toggle button in the header/name Card (next to the risk badge/health line), revealing the history timeline inline within that same card, collapsed by default -- reusing RecommendationCard's existing expand/collapse pattern (feat-036: local useState, ChevronDown/ChevronUp swap, aria-expanded) rather than introducing a new interaction style.",
      "user_visible_behavior": "Asset Detail no longer shows a separate 'Today's Tasks' card (Recommendations already covers the same ground with full detail). History is hidden by default behind a small 'History' button in the name/header card, and expands inline in place when clicked.",
      "dependencies": ["feat-017", "feat-036"],
      "status": "passing",
      "verification": [
        "cd frontend && npx tsc --noEmit && npm run lint && npm run build all succeed",
        "Live Playwright check against the real backend + live Snowflake account: load a real asset detail page, confirm no 'Today's Tasks' heading is present and 'Recommendations' still is; confirm exactly one History toggle button exists and history content is not present in the page text until clicked; click it and confirm the page's text grows to include real history content, click again and confirm it collapses back; zero console errors. Screenshot both collapsed and expanded states."
      ],
      "evidence": [
        "npx tsc --noEmit: clean. npm run lint: clean (removed now-unused ClipboardList import). npm run build: clean, all 5 routes compiled successfully.",
        "Live Playwright check against /assets/FP-001 (real backend + live Snowflake account): page text contained zero occurrences of \"Today's Tasks\" and did contain \"Recommendations\"; exactly 1 History toggle button found. Rendered body text length was 2790 chars collapsed, grew to 3038 chars after clicking History (real Q2/Q3/Q4-2024 biomass history content revealed), then returned to 2790 chars after clicking again to collapse -- confirms real two-way toggle behavior, not a one-shot reveal. Zero console/page errors.",
        "Screenshots (collapsed and expanded) confirm the visual result: header card shows a compact 'History' pill button with a chevron next to the asset name/risk badge; expanding it inline-reveals the real timeline ('Q4-2024 -- biomass kg harvested: 145 -- Severe DO crash in Oct, 40% stock loss before recovery', etc.) directly under the header's divider, pushing Live Sensor Readings/AI Prediction/Recommendations down naturally; collapsing hides it again with no leftover empty space."
      ],
      "notes": "Confirmed via code (not just visual judgment) that Today's Tasks and Recommendations were literally the same underlying fetch before removing it -- see lib/api.ts:392-397's own comment: \"today's tasks framed as this asset's pending recommendations\"."
    }
  ],
  "completed_note": "2026-07-15: feat-008 through feat-029 (22 features -- the full FarmTwin pivot, the performance/split-screen-UX batch, and the visual-overhaul batch) all reached 'passing' with real runtime evidence and were removed from this active list at the user's explicit request, since they are done and were cluttering the list of upcoming work. Nothing was deleted from the repository or lost: every one of those features' full evidence trail (implementation details, verification commands run, real output, screenshots referenced) remains in progress.md's session-by-session log (Sessions 011 through 019). This mirrors the same precedent set by the 2026-07-14 pivot_note below, which did the same thing for the original feat-001 through feat-007."
}
