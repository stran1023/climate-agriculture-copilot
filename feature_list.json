{
  "project": "farmtwin-ai-copilot",
  "last_updated": "2026-07-14",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "do_not_skip_verification": true
  },
  "status_legend": {
    "not_started": "Work has not begun.",
    "in_progress": "The feature is the current active task.",
    "blocked": "Work cannot continue until a documented blocker is resolved.",
    "passing": "Required verification has passed and evidence is recorded."
  },
  "pivot_note": "2026-07-14: project rewritten around docs/FarmTwin-AI-Copilot.md (single farm, 4 heterogeneous Farm Assets, isometric digital twin, AI-Copilot-centric UI, structured 6-field recommendations). This replaces the prior rice-cooperative build (feat-001 through feat-007, formerly all 'passing'). That build's full evidence trail is preserved in progress.md under 'Legacy: rice-cooperative build (superseded 2026-07-14)' -- nothing was deleted, the roadmap moved forward. See docs/architecture.md and docs/ui-build-plan.md for the current target design.",
  "features": [
    {
      "id": "feat-008",
      "priority": 1,
      "area": "snowflake",
      "name": "Rebuild Snowflake schema for the FarmTwin asset model",
      "description": "Via CoCo, replace FARMS/SENSOR_READINGS/RISK_ASSESSMENTS/WORK_ORDERS/CROP_HISTORY with FARM_ASSETS, ASSET_READINGS, ASSET_RISK_ASSESSMENTS, RECOMMENDATIONS, ASSET_HISTORY per the schema table in docs/architecture.md. WEATHER_READINGS is kept but becomes farm-wide (no farm_id). Seed FARM_ASSETS with exactly 4 rows (fish_pond, chicken_coop, rice_field, fruit_orchard) and generate realistic evolving time-series seed data for ASSET_READINGS/ASSET_RISK_ASSESSMENTS/ASSET_HISTORY per the vision doc's per-asset simulated-data list.",
      "user_visible_behavior": "Querying the new tables in Snowsight returns the expected schema and seed rows for exactly 4 assets.",
      "dependencies": [],
      "status": "not_started",
      "verification": [
        "Every new prompt added to snowflake/coco-prompts.md has its 'Result' line filled in",
        "SELECT * FROM FARM_ASSETS returns exactly 4 rows, one per asset_type",
        "SELECT * from each new table returns non-empty, type-appropriate seed rows without error"
      ],
      "evidence": [],
      "notes": "Prerequisite for every other feature below. This is a destructive rebuild of the live OPS schema (drops the old rice-farm tables) -- run interactively via CoCo per CLAUDE.md, do not attempt to script this from the FastAPI backend."
    },
    {
      "id": "feat-009",
      "priority": 2,
      "area": "snowflake",
      "name": "Rebuild semantic view + Cortex Agent for structured, asset-aware recommendations",
      "description": "Via CoCo, rebuild the semantic view over FARM_ASSETS/ASSET_READINGS/ASSET_RISK_ASSESSMENTS/RECOMMENDATIONS/ASSET_HISTORY, then rebuild the Cortex Agent so its responses can be parsed into the 6-field Recommendation/Reason/Evidence/Priority/Expected Impact/Confidence format mandated by docs/FarmTwin-AI-Copilot.md, per asset, instead of one shared free-text narrative.",
      "user_visible_behavior": "Asking the agent about a specific asset returns a response containing all 6 required fields, grounded in that asset's real Snowflake data.",
      "dependencies": ["feat-008"],
      "status": "not_started",
      "verification": [
        "A test prompt against the rebuilt agent returns a response containing all 6 fields for a specific asset",
        "The semantic view answers at least 3 verified onboarding-style questions correctly (mirroring the rigor of the original FARM_OPS_VIEW verification)"
      ],
      "evidence": [],
      "notes": "Decide during implementation whether the agent emits structured JSON directly (preferred, avoids the old build's fragile 'match farm name as a substring of free text' heuristic) or structured-but-parseable prose that the backend regex/parses into the 6 fields."
    },
    {
      "id": "feat-010",
      "priority": 3,
      "area": "backend",
      "name": "New Pydantic models + Snowflake read/write layer for the asset schema",
      "description": "Replace backend/app/models/schemas.py's Plot/PlotRisk/WorkOrder/RiskAssessment/DailyBriefing/BriefingToday with FarmAsset, AssetReading, AssetRisk, Recommendation (6-field), DailyBriefing v2, BriefingToday v2. Add the corresponding query/insert/update functions in snowflake_client.py usage sites for the new tables.",
      "user_visible_behavior": "No visible behavior change yet (no endpoints wired) -- this is the typed foundation the next features build on.",
      "dependencies": ["feat-008"],
      "status": "not_started",
      "verification": [
        "cd backend && python -m compileall app",
        "A standalone script (or Python REPL) using the new models successfully runs a SELECT against FARM_ASSETS and ASSET_READINGS and constructs valid FarmAsset/AssetReading instances from the rows"
      ],
      "evidence": [],
      "notes": "Old Plot/PlotRisk/WorkOrder models and the /plots, /plots/{id}/risk, /workorders/{id}/approve|reject endpoints in main.py should be removed in this feature or the next, not left dangling against dropped tables."
    },
    {
      "id": "feat-011",
      "priority": 4,
      "area": "backend",
      "name": "Simulation engine for per-asset sensor data",
      "description": "No physical IoT exists. Build a simulation step (module under backend/app/services/, e.g. asset_simulator.py) that generates realistic, evolving values for each asset type's ASSET_READINGS columns on every /workflow/run call -- values should trend (not be purely random each run) so risk stories are visible over multiple runs, per docs/FarmTwin-AI-Copilot.md's 'simulated data should behave realistically over time.'",
      "user_visible_behavior": "Each POST /workflow/run call (once wired in feat-012) writes one new ASSET_READINGS row per asset, and values drift plausibly run over run rather than jumping randomly.",
      "dependencies": ["feat-010"],
      "status": "not_started",
      "verification": [
        "cd backend && python -m compileall app",
        "Calling the simulator function directly across 5+ simulated ticks for one asset shows a plausible trend (e.g. dissolved oxygen trending down, not random noise) matching a hand-picked scenario"
      ],
      "evidence": [],
      "notes": "Keep the trend logic simple (e.g. a persisted last-value + bounded random walk, or a scripted scenario for the one asset used in the demo) -- do not over-engineer a full simulation framework for a hackathon build."
    },
    {
      "id": "feat-012",
      "priority": 5,
      "area": "backend",
      "name": "Rewrite /workflow/run as Observe -> Understand -> Recommend -> Predict",
      "description": "Replace run_daily_workflow's rice-farm logic with the 4-stage cycle per asset: Observe (run the feat-011 simulator + ingest farm-wide weather), Understand (write ASSET_RISK_ASSESSMENTS per asset via the feat-009 agent), Recommend (write RECOMMENDATIONS rows in the full 6-field structured format), Predict (a short-horizon prediction, stored either as an ASSET_RISK_ASSESSMENTS row with a future-looking risk_type or a dedicated notes field -- decide during implementation and record the decision in docs/architecture.md).",
      "user_visible_behavior": "POST /workflow/run writes real ASSET_READINGS, ASSET_RISK_ASSESSMENTS, and RECOMMENDATIONS rows for all 4 assets and returns a DailyBriefing v2 reflecting real counts, not hardcoded values.",
      "dependencies": ["feat-009", "feat-011"],
      "status": "not_started",
      "verification": [
        "uvicorn app.main:app --reload, then curl -X POST localhost:8000/workflow/run",
        "New rows appear in ASSET_READINGS, ASSET_RISK_ASSESSMENTS, and RECOMMENDATIONS for all 4 assets with today's timestamp",
        "At least one RECOMMENDATIONS row has all 6 fields populated with non-placeholder content"
      ],
      "evidence": [],
      "notes": "This is the direct replacement for the old feat-002/003/004 pipeline, now covering 4 heterogeneous assets in one pass instead of N identical rice farms."
    },
    {
      "id": "feat-013",
      "priority": 6,
      "area": "backend",
      "name": "Asset read endpoints + recommendation approve/reject + dashboard summary + briefing",
      "description": "Add GET /assets (list with latest health/status/risk), GET /assets/{id} (readings + risk + history), GET /assets/{id}/recommendations (structured cards), POST /recommendations/{id}/approve and /reject (write-back to RECOMMENDATIONS), GET /dashboard/summary (farm health score, active alerts derived from ASSET_RISK_ASSESSMENTS, tasks due today derived from pending RECOMMENDATIONS, weather, top recommendations, asset overview), and rebuild GET /briefing/today against RECOMMENDATIONS instead of WORK_ORDERS. Remove the old /plots, /plots/{id}/risk, /workorders/{id}/approve|reject endpoints.",
      "user_visible_behavior": "Every endpoint in docs/ui-build-plan.md's data contract table (except /copilot/ask) returns real Snowflake-backed data; approving/rejecting a recommendation flips its status and it disappears from the pending list and dashboard's Tasks Due Today.",
      "dependencies": ["feat-012"],
      "status": "not_started",
      "verification": [
        "curl GET /assets, /assets/{id}, /assets/{id}/recommendations, /dashboard/summary, /briefing/today all return real Snowflake-backed data",
        "POST /recommendations/{id}/approve and /reject update the row and are reflected in a subsequent GET",
        "GET /assets/999/... style calls for a non-existent id return 404"
      ],
      "evidence": [],
      "notes": "Direct replacement for old feat-004/006's endpoint set, generalized from farms/plots to heterogeneous assets."
    },
    {
      "id": "feat-014",
      "priority": 7,
      "area": "backend",
      "name": "AI Copilot free-form Q&A endpoint",
      "description": "Add POST /copilot/ask: takes a free-form question, calls the Cortex Agent grounded in current farm state via the semantic view, and returns an answer that ends with actionable next steps per the vision doc's 'Decision Intelligence' principle. Must handle the example questions in docs/FarmTwin-AI-Copilot.md verbatim (e.g. 'What should I do today?', 'Should I feed the fish?').",
      "user_visible_behavior": "POST /copilot/ask with a natural-language farm question returns a grounded, non-generic answer referencing real current asset state.",
      "dependencies": ["feat-009", "feat-013"],
      "status": "not_started",
      "verification": [
        "curl -X POST /copilot/ask with each of the vision doc's example questions returns a response that references specific, real asset data (not generic agricultural advice)",
        "Response text ends with a concrete next step, not just an observation"
      ],
      "evidence": [],
      "notes": "This is the doc's clearest anti-pattern check: 'Rice generally needs watering' (bad) vs. a response grounded in this farm's actual current state (good). Verification should explicitly compare against that bar."
    },
    {
      "id": "feat-015",
      "priority": 8,
      "area": "frontend",
      "name": "Digital Twin home -- isometric farm map (replaces the real Leaflet/OSM Screen 1)",
      "description": "Build Screen 1 per docs/ui-build-plan.md: isometric 2D layout with one interactive object per asset positioned via grid_x/grid_y, color-coded green/yellow/red by risk level, hover popover (name/health/status/latest alert), click navigates to the asset detail screen (feat-017). Removes frontend/components/FarmMap.tsx (Leaflet) and its next/dynamic wiring in app/page.tsx.",
      "user_visible_behavior": "Opening localhost:3000 shows 4 isometric farm-asset objects (not a real-world map), color-coded by risk, with working hover and click-through.",
      "dependencies": ["feat-013"],
      "status": "not_started",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Manual/Playwright walkthrough: load /, confirm all 4 assets render with correct colors matching GET /assets, hover shows the popover, click navigates to the correct asset detail page"
      ],
      "evidence": [],
      "notes": "Rendering approach (CSS/SVG vs. a library) is an open decision -- see docs/ui-build-plan.md Screen 1 section. Leaflet/react-leaflet/@types/leaflet become unused dependencies once this lands; remove them from package.json in this feature, not left dangling."
    },
    {
      "id": "feat-016",
      "priority": 9,
      "area": "frontend",
      "name": "Farm Dashboard screen",
      "description": "Build Screen 2 per docs/ui-build-plan.md: overall farm health score, active alerts, tasks due today, farm statistics, weather + forecast, daily recommendations (top N, priority-sorted), asset status overview. Reads GET /dashboard/summary.",
      "user_visible_behavior": "A dashboard screen answers 'how is my farm doing today' within a few seconds of loading, with real counts and a real weather forecast.",
      "dependencies": ["feat-013"],
      "status": "not_started",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Manual walkthrough: load the dashboard route, confirm every displayed number/badge matches a direct curl of GET /dashboard/summary"
      ],
      "evidence": [],
      "notes": null
    },
    {
      "id": "feat-017",
      "priority": 10,
      "area": "frontend",
      "name": "Asset detail screen",
      "description": "Build Screen 3 per docs/ui-build-plan.md: simulated sensor values (type-specific, labeled 'simulated'), AI analysis narrative, one recommendation card per pending recommendation (full 6-field format with Approve/Reject), today's tasks, prediction, and history. Reads GET /assets/{id} and GET /assets/{id}/recommendations; writes via POST /recommendations/{id}/approve|reject.",
      "user_visible_behavior": "Clicking an asset on the digital twin or dashboard opens a detail view showing that asset's real sensor readings, AI reasoning, and actionable recommendation cards; approving/rejecting one updates the panel in place.",
      "dependencies": ["feat-013"],
      "status": "not_started",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Manual/Playwright walkthrough: open one asset of each type, confirm type-specific fields render correctly, approve one recommendation and confirm the panel updates and Snowflake's RECOMMENDATIONS row actually flips status (cross-check via curl)"
      ],
      "evidence": [],
      "notes": "Direct replacement for the old Screen 2 (plots/[id]/page.tsx), generalized across 4 asset types instead of one crop type."
    },
    {
      "id": "feat-018",
      "priority": 11,
      "area": "frontend",
      "name": "AI Copilot panel",
      "description": "Build Screen 4 per docs/ui-build-plan.md: a persistent, prominent surface (side panel or dedicated route) showing a prioritized cross-asset recommendation feed plus a free-form question box wired to POST /copilot/ask. This is the vision doc's explicit centerpiece requirement -- 'the application should never feel like a dashboard with a chatbot attached.'",
      "user_visible_behavior": "A farmer can type 'What should I do today?' or click through the example questions from docs/FarmTwin-AI-Copilot.md and get a grounded, actionable answer, from any screen in the app.",
      "dependencies": ["feat-014"],
      "status": "not_started",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Manual/Playwright walkthrough: from at least 2 different screens, open the copilot, ask 2+ of the vision doc's example questions, confirm grounded (not generic) answers render"
      ],
      "evidence": [],
      "notes": "This feature is what separates FarmTwin from a monitoring dashboard per the vision doc -- treat its verification bar as strict, not perfunctory."
    },
    {
      "id": "feat-019",
      "priority": 12,
      "area": "frontend",
      "name": "Daily briefing screen (rebuilt)",
      "description": "Rebuild the existing briefing screen (frontend/app/briefing/page.tsx) against the new GET /briefing/today (RECOMMENDATIONS-based instead of WORK_ORDERS-based) -- same shape and purpose as before, updated data source and field names only.",
      "user_visible_behavior": "The briefing screen shows today's approved/rejected recommendations across all 4 assets plus a generated summary.",
      "dependencies": ["feat-013"],
      "status": "not_started",
      "verification": [
        "npm run build && npm run lint succeed in frontend/",
        "Manual walkthrough: approve and reject at least one recommendation each, confirm both appear correctly in the briefing screen with a real Cortex-generated summary"
      ],
      "evidence": [],
      "notes": "Lowest-risk of the frontend features -- mostly a data-source swap on an already-proven screen pattern."
    }
  ]
}
