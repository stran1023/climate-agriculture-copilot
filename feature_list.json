{
  "project": "climate-adaptive-agriculture-copilot",
  "last_updated": "2026-07-13",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "do_not_skip_verification": true
  },
  "status_legend": {
    "not_started": "Work has not begun.",
    "in_progress": "The feature is the current active task.",
    "blocked": "Work cannot continue until a documented blocker is resolved.",
    "passing": "Required verification has passed and evidence is recorded."
  },
  "features": [
    {
      "id": "feat-001",
      "priority": 1,
      "area": "snowflake",
      "name": "Snowflake objects built via CoCo",
      "description": "Run the prompts in snowflake/coco-prompts.md against a real Snowflake account: CLIMATE_AG_COPILOT database/OPS schema, the FARMS/WEATHER_READINGS/SENSOR_READINGS/RISK_ASSESSMENTS/WORK_ORDERS/CROP_HISTORY tables, and seed data (including historical yield seasons). This is a prerequisite for every backend feature below.",
      "user_visible_behavior": "Querying the tables in Snowsight returns the expected schema and seed rows.",
      "dependencies": [],
      "status": "passing",
      "verification": [
        "Every prompt in snowflake/coco-prompts.md has its 'Result' line filled in",
        "SELECT * from each table in CLIMATE_AG_COPILOT.OPS returns rows without error"
      ],
      "evidence": [
        "2026-07-13: All 5 prompts in snowflake/coco-prompts.md have 'Result' lines filled in (db/tables, seed data, semantic view, Cortex Agent, verification queries).",
        "2026-07-13: backend/.env populated with real SNOWFLAKE_ACCOUNT/USER/ROLE/WAREHOUSE/DATABASE/SCHEMA/PAT. Created backend/venv, installed requirements.txt, and ran a live query via app.services.snowflake_client.run_query() against each table: FARMS=15, WEATHER_READINGS=450, SENSOR_READINGS=450, RISK_ASSESSMENTS=72, CROP_HISTORY=45 rows (all match coco-prompts.md counts). WORK_ORDERS=0 rows, which is expected — that table is seeded by app logic in feat-004, not by the CoCo seed-data prompt."
      ],
      "notes": "Verified against the real Snowflake account this session. WORK_ORDERS is empty by design until feat-004 writes to it."
    },
    {
      "id": "feat-002",
      "priority": 2,
      "area": "backend",
      "name": "Wire weather ingestion + Snowflake write into /workflow/run",
      "description": "Replace the stub in backend/app/main.py:run_daily_workflow (steps 1-2) with real calls: fetch per-farm forecasts via app/services/weather_client.py, then write WEATHER_READINGS rows via app/services/snowflake_client.py.",
      "user_visible_behavior": "POST /workflow/run writes real weather rows to WEATHER_READINGS for every farm in FARMS.",
      "dependencies": ["feat-001"],
      "status": "passing",
      "verification": [
        "uvicorn app.main:app --reload, then curl -X POST localhost:8000/workflow/run",
        "New rows appear in WEATHER_READINGS with today's timestamp"
      ],
      "evidence": [
        "2026-07-13: Added weather_client.get_today_reading() (reduces Open-Meteo forecast to today's rainfall/temp/humidity) and snowflake_client.execute_many() (INSERT via executemany). Wired both into run_daily_workflow steps 1-2: query FARMS, fetch a live forecast per farm, bulk-insert into WEATHER_READINGS.",
        "2026-07-13: Ran uvicorn app.main:app against the real backend/venv + live Snowflake account. WEATHER_READINGS row count was 450 before POST /workflow/run, 465 after (+15, one per seeded farm). curl response: {\"farms_assessed\":15,...}. Sampled the 5 newest rows via a direct query — real Open-Meteo values (e.g. FARM_ID 15: rainfall_mm=16.2, temp_c=26.65, humidity_pct=92.08) with today's timestamp and source='open-meteo'."
      ],
      "notes": "Steps 3-5 of run_daily_workflow (risk assessment, work orders, real briefing summary) remain stubbed — that's feat-003/004/006."
    },
    {
      "id": "feat-003",
      "priority": 3,
      "area": "backend",
      "name": "Cortex Agent risk assessment call",
      "description": "backend/app/services/cortex_agent_client.py is a placeholder pending the exact Cortex Agents REST API request/response shape (see its module docstring). Confirm the real endpoint/payload against Snowflake's docs and the FARM_OPS_AGENT built in feat-001, then wire step 3 of run_daily_workflow to call it and parse risk + recommendations per farm.",
      "user_visible_behavior": "POST /workflow/run returns real flood/drought/disease risk levels per farm instead of an empty list.",
      "dependencies": ["feat-001", "feat-002"],
      "status": "passing",
      "verification": [
        "ask_agent() returns a real response from FARM_OPS_AGENT for a sample prompt",
        "run_daily_workflow populates high_risk_farms from the agent's assessment, not a hardcoded value"
      ],
      "evidence": [
        "2026-07-13: Confirmed the real Cortex Agents Run API shape via Snowflake docs (docs.snowflake.com/en/user-guide/snowflake-cortex/cortex-agents-run) — agent-object endpoint `POST /api/v2/databases/{db}/schemas/{schema}/agents/{name}:run`, request body `{messages:[{role,content:[{type:'text',text}]}], stream:false}`, response `{content:[{type:'text',text},...], status,...}`. Rewrote cortex_agent_client.py's placeholder to match.",
        "2026-07-13: Called ask_agent() directly against the live FARM_OPS_AGENT with prompt 'Which farms are at high flood risk this week?' — got a real 1099-character response identifying the same 4 CRITICAL flood-risk farms and recommendations documented in coco-prompts.md step 5, proving the endpoint/payload/response-parsing are all correct.",
        "2026-07-13: Wired step 3 into run_daily_workflow (queries FARM_ID+NAME, asks FARM_OPS_AGENT for current at-risk farms, matches farm names in the narrative to build high_risk_farms). Ran uvicorn against the live account and POSTed /workflow/run: response was `high_risk_farms: ['1','2','3','4']` with a real narrative in `summary` describing CRITICAL flood risk and drainage recommendations — matches the seeded flood-risk farms exactly, not hardcoded. WEATHER_READINGS also still grew 465->480 in the same run, confirming feat-002 wiring is undisturbed."
      ],
      "notes": "high_risk_farms is derived by matching each farm's NAME as a substring of the agent's free-text narrative — a pragmatic heuristic for this hackathon scope rather than structured tool output parsing."
    },
    {
      "id": "feat-004",
      "priority": 4,
      "area": "backend",
      "name": "Plot/risk read endpoints + work order approval endpoints",
      "description": "Step 4 of run_daily_workflow: for farms flagged high-risk by feat-003, create WORK_ORDERS rows with status pending_approval. Add the read/write endpoints the frontend contract in docs/ui-build-plan.md needs: GET /plots (plot id, name, location, latest risk level), GET /plots/{id}/risk (risk narrative + generated work order), POST /workorders/{id}/approve and POST /workorders/{id}/reject (update WORK_ORDERS.status, approved_by, approved_at).",
      "user_visible_behavior": "GET /plots lists plots with risk badges; GET /plots/{id}/risk returns the Cortex Agent's narrative + recommended work order; calling approve/reject flips a work order's status and it disappears from the pending list.",
      "dependencies": ["feat-003"],
      "status": "passing",
      "verification": [
        "curl -X POST /workflow/run creates WORK_ORDERS rows for seeded high-risk farms",
        "curl GET /plots and /plots/{id}/risk return real Snowflake-backed data",
        "Approve/reject endpoint updates the row and is reflected in a subsequent GET"
      ],
      "evidence": [
        "2026-07-13: Added snowflake_client.execute() (single UPDATE/INSERT with rowcount), Plot/PlotRisk/ApprovalRequest models in schemas.py, and wired step 4 of run_daily_workflow to bulk-insert WORK_ORDERS rows (status='pending_approval', action=the step-3 agent narrative) for every farm_id in high_risk_farms, using MAX(WORK_ORDER_ID)+1 for IDs since the column has no identity/sequence.",
        "2026-07-13: Added GET /plots (latest RISK_ASSESSMENTS row per farm via QUALIFY, overall risk_level = max severity of flood/drought/disease), GET /plots/{id}/risk (latest RISK_ASSESSMENTS.NOTES as narrative + latest WORK_ORDERS row, 404 if no risk assessment exists for that farm), POST /workorders/{id}/approve and /reject (UPDATE status+approved_by+approved_at, 404 if the id doesn't exist).",
        "2026-07-13: Ran uvicorn against the live account. POST /workflow/run created WORK_ORDERS ids 5-8 (one per high-risk farm 1-4, status='pending_approval', WORK_ORDERS count 0->4). GET /plots returned all 15 farms with farms 1-4 correctly showing risk_level='critical' and the rest 'low'/'medium'. GET /plots/1/risk returned the real RISK_ASSESSMENTS narrative + work order 5. POST /workorders/5/approve (with body {\"approved_by\":\"test_manager\"}) and /workorders/6/reject (default approved_by) both updated status+approved_by+approved_at, confirmed via a follow-up GET /plots/1/risk showing work_order.status='approved'. GET /plots/999/risk and POST /workorders/9999/approve both correctly returned 404."
      ],
      "notes": "work_order_id is Snowflake NUMBER with no identity/sequence, so IDs are assigned client-side via MAX+1 -- fine for this single-process demo, not safe under concurrent writers. All 4 work orders created in one /workflow/run share the same action text (the one combined agent narrative from feat-003), not a per-farm-specific recommendation."
    },
    {
      "id": "feat-005",
      "priority": 5,
      "area": "frontend",
      "name": "Three-screen frontend (plot list, risk + work order, approval history/briefing)",
      "description": "Scaffold frontend/ per frontend/README.md (create-next-app). Build the 3 screens defined in docs/ui-build-plan.md, in that doc's build order: Screen 2 (risk assessment + work order panel, approve/reject) first against feat-004's /plots/{id}/risk and approve/reject endpoints, then Screen 1 (plot list) against /plots, then Screen 3 (approval history + daily briefing) against feat-006's /briefing/today.",
      "user_visible_behavior": "Opening localhost:3000 shows the plot list; clicking a plot shows the Cortex Agent's risk narrative and recommended work order with approve/reject; a third screen shows today's approved/rejected work orders plus a generated daily briefing.",
      "dependencies": ["feat-004", "feat-006"],
      "status": "not_started",
      "verification": [
        "npm run build succeeds in frontend/",
        "Manual walkthrough: load /, open a plot, approve its work order, confirm it appears in the approval history screen"
      ],
      "evidence": [],
      "notes": "frontend/ has no package.json yet — this feature includes the initial scaffold, not just the pages."
    },
    {
      "id": "feat-006",
      "priority": 6,
      "area": "backend",
      "name": "Daily briefing summary assembly + /briefing/today endpoint",
      "description": "Step 5 of run_daily_workflow: replace the hardcoded DailyBriefing fields (date, farms_assessed, summary) with real values derived from feat-002/003/004 results. Add GET /briefing/today, which aggregates today's approved/rejected work orders and returns the generated summary, per the contract in docs/ui-build-plan.md.",
      "user_visible_behavior": "The DailyBriefing returned by /workflow/run accurately reflects the day's real farms_assessed count, high_risk_farms, and work_orders_created; GET /briefing/today returns the same day's approved/rejected list plus a natural-language summary.",
      "dependencies": ["feat-002", "feat-003", "feat-004"],
      "status": "not_started",
      "verification": [
        "curl -X POST /workflow/run and confirm every field matches what was actually written to Snowflake this run",
        "curl GET /briefing/today after approving at least one work order and confirm it reflects that approval"
      ],
      "evidence": [],
      "notes": ""
    }
  ]
}
