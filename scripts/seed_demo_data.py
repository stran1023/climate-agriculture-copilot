"""
Optional local seed script — mostly superseded by asking CoCo CLI to
generate sample data directly in Snowflake (see snowflake/coco-prompts.md,
step 2). Keep this only if you need data generated outside Snowflake,
e.g. to feed the weather_client during offline demo runs.
"""
